import styled from 'styled-components';
const StyledPokedexBox = styled.div`
    display: flex;
    margin-top: 5rem;

    gap: 1.2rem;
    justify-content: center;
    .table {
        display: flex;
        justify-content: center;
        gap: 2rem;
        padding: 2rem;
        margin: 1rem;
        flex-flow: wrap;
    }
`;

export default StyledPokedexBox;
